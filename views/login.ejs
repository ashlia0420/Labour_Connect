<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - LabourConnect</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    
    <style>
        /* Global Styles */
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            transition: background 0.3s, color 0.3s;
        }

        /* Navbar */
        .navbar {
            padding: 15px;
        }

        .navbar .navbar-brand {
            font-size: 24px;
            font-weight: bold;
            color: white;
        }

        .navbar .nav-link {
            color: white !important;
            transition: 0.3s;
        }

        .navbar .nav-link:hover {
            background-color: white;
            color: #709775 !important;
            border-radius: 5px;
        }

        /* Dark Mode Toggle */
        .darkModeToggle {
            width: 40px;
            height: 40px;
            background: transparent;
            border: none;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: transform 0.3s ease;
        }

        .darkModeToggle i {
            font-size: 22px;
            color: white;
        }

        .darkModeToggle:hover {
            transform: scale(1.2);
        }

        /* Login Form */
        .login-form {
            max-width: 600px;
            margin: 50px auto;
            background: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .login-form input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .login-form .btn {
            background-color: #4da8ff;
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            transition: 0.3s;
        }

        .login-form .btn:hover {
            background-color: #007bff;
        }

        .create-account {
            margin-top: 10px;
        }

        /* Dark Mode Styles */
        body.dark-mode {
            background-color: #2c2c2c;
            color: white;
        }

        body.dark-mode .navbar {
            background-color: #222 !important;
        }

        body.dark-mode .nav-link {
            color: #ffffff !important;
        }

        body.dark-mode .login-form {
            background: #333;
            color: white;
        }

        body.dark-mode .login-form input {
            background: #555;
            border: 1px solid #777;
            color: white;
        }

        body.dark-mode .login-form .btn {
            background-color: #6a8d56;
        }

        body.dark-mode .login-form .btn:hover {
            background-color: #4d6f39;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-dark border-bottom border-body" data-bs-theme="dark">
        <div class="container-fluid">
            <a class="navbar-brand text-white" href="#">Labour Connect</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-between" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                    <a class="nav-link active text-white" href="/labour">Home</a>
                    <a class="nav-link text-white" href="/contact">Contact</a>
                </div>
                <button class="darkModeToggle btn btn-outline-light">
                    <i class="fas fa-moon"></i>
                </button>
            </div>
        </div>
    </nav>
  
    
    <section class="login-form">
        <h2>Login</h2>
        <form action="/login/rent" method="post">
            <label for="username"></label>
            <input type="text" name="username" placeholder="Username" id="username" required>  <!-- ADDED name="username" -->
            <label for="password"></label>
            <input type="password" name="password" placeholder="Password" id="password" required>  <!-- ADDED name="password" -->
            <button type="submit" class="btn">Login</button>
        </form>
        
        <p class="create-account">Don't have an account? <a href="/create">Create one</a></p>
    </section>
    
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const darkModeToggle = document.querySelector('.darkModeToggle');
            const body = document.body;
    
            if (localStorage.getItem('theme') === 'dark') {
                body.classList.add('dark-mode');
            }
    
            darkModeToggle.addEventListener('click', () => {
                body.classList.toggle('dark-mode');
                localStorage.setItem('theme', body.classList.contains('dark-mode') ? 'dark' : 'light');
            });
        });
    </script>
</body>
</html>

 
